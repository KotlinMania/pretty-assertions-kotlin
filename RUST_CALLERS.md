# Rust callers — `pretty-assertions-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `pretty_assertions` | `pretty_assertions` | 861 | 89 | 7 | 713 |

## `pretty_assertions`

### Inbound symbols (2 distinct, 861 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `pretty_assertions`.

#### `pretty_assertions::assert_eq` — 858 site(s) (713 codex, 145 other, 89 caller crates)

- ✅ `codex-kotlin/codex-agent-graph-store` `src/local.rs:113` — `use pretty_assertions::assert_eq`
- ✅ `codex-kotlin/codex-agent-graph-store` `src/types.rs:17` — `use pretty_assertions::assert_eq`
- ✅ `codex-kotlin/codex-agent-identity` `src/lib.rs:409` — `use pretty_assertions::assert_eq`
- ✅ `codex-kotlin/codex-analytics` `src/analytics_client_tests.rs:107` — `use pretty_assertions::assert_eq`
- ✅ `codex-kotlin/codex-api` `src/api_bridge_tests.rs:3` — `use pretty_assertions::assert_eq`
- ✅ `codex-kotlin/codex-api` `src/endpoint/memories.rs:83` — `use pretty_assertions::assert_eq`
- ✅ `codex-kotlin/codex-api` `src/endpoint/models.rs:88` — `use pretty_assertions::assert_eq`
- ✅ `codex-kotlin/codex-api` `src/endpoint/realtime_call.rs:236` — `use pretty_assertions::assert_eq`
- ✅ `codex-kotlin/codex-api` `src/endpoint/realtime_websocket/methods.rs:841` — `use pretty_assertions::assert_eq`
- ✅ `codex-kotlin/codex-api` `src/endpoint/responses_websocket.rs:677` — `use pretty_assertions::assert_eq`
- ✅ `codex-kotlin/codex-api` `src/files.rs:279` — `use pretty_assertions::assert_eq`
- ✅ `codex-kotlin/codex-api` `src/rate_limits.rs:264` — `use pretty_assertions::assert_eq`
- _… and 846 more site(s)._

#### `pretty_assertions::assert_ne` — 3 site(s) (0 codex, 3 other, 1 caller crates)

- `os-info-kotlin/os_info` `src/bitness.rs:119` — `use pretty_assertions::assert_ne`
- `os-info-kotlin/os_info` `src/macos/mod.rs:120` — `use pretty_assertions::assert_ne`
- `os-info-kotlin/os_info` `src/windows/winapi.rs:322` — `use pretty_assertions::assert_ne`

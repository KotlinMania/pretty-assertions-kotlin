# Port targets — `pretty-assertions-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `pretty_assertions` that codex-rs uses, and therefore the minimum subset that `pretty-assertions-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-100] (rank `281`, tier `direct`, status `To Do`)

**Cargo package name**: `pretty_assertions`
**Rust module name (in `use` statements)**: `pretty_assertions`

## Summary

- **1** distinct symbols imported by codex-rs
- **713** total `use` statements
- **694** codex source files affected
- **81** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `pretty_assertions::assert_eq`

- **713 `use` site(s)** across **81 codex member crate(s)**: `codex-agent-graph-store`, `codex-agent-identity`, `codex-analytics`, `codex-api`, `codex-app-server`, …

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `agent-graph-store/src/local.rs:113` (`codex-agent-graph-store`) — `use pretty_assertions::assert_eq`
- `agent-graph-store/src/types.rs:17` (`codex-agent-graph-store`) — `use pretty_assertions::assert_eq`
- `agent-identity/src/lib.rs:409` (`codex-agent-identity`) — `use pretty_assertions::assert_eq`
- `analytics/src/analytics_client_tests.rs:107` (`codex-analytics`) — `use pretty_assertions::assert_eq`
- `app-server-client/src/lib.rs:959` (`codex-app-server-client`) — `use pretty_assertions::assert_eq`
- `app-server-protocol/src/experimental_api.rs:64` (`codex-app-server-protocol`) — `use pretty_assertions::assert_eq`
- `app-server-protocol/src/export.rs:2093` (`codex-app-server-protocol`) — `use pretty_assertions::assert_eq`
- `app-server-protocol/src/protocol/common.rs:1477` (`codex-app-server-protocol`) — `use pretty_assertions::assert_eq`
- `app-server-protocol/src/protocol/common_tests.rs:4` (`codex-app-server-protocol`) — `use pretty_assertions::assert_eq`
- `app-server-protocol/src/protocol/event_mapping.rs:445` (`codex-app-server-protocol`) — `use pretty_assertions::assert_eq`
- `app-server-protocol/src/protocol/thread_history.rs:1228` (`codex-app-server-protocol`) — `use pretty_assertions::assert_eq`
- `app-server-protocol/src/schema_fixtures.rs:340` (`codex-app-server-protocol`) — `use pretty_assertions::assert_eq`
- `app-server-transport/src/transport/mod.rs:272` (`codex-app-server-transport`) — `use pretty_assertions::assert_eq`
- `app-server-transport/src/transport/remote_control/client_tracker.rs:340` (`codex-app-server-transport`) — `use pretty_assertions::assert_eq`
- `app-server-transport/src/transport/remote_control/enroll.rs:263` (`codex-app-server-transport`) — `use pretty_assertions::assert_eq`
- `app-server-transport/src/transport/remote_control/protocol.rs:202` (`codex-app-server-transport`) — `use pretty_assertions::assert_eq`
- `app-server-transport/src/transport/remote_control/segment_tests.rs:17` (`codex-app-server-transport`) — `use pretty_assertions::assert_eq`
- `app-server-transport/src/transport/remote_control/tests.rs:37` (`codex-app-server-transport`) — `use pretty_assertions::assert_eq`
- `app-server-transport/src/transport/remote_control/websocket.rs:1231` (`codex-app-server-transport`) — `use pretty_assertions::assert_eq`
- `app-server-transport/src/transport/unix_socket_tests.rs:13` (`codex-app-server-transport`) — `use pretty_assertions::assert_eq`
- _… and 693 more (see `codex_external_usage.json` for the full list)._
